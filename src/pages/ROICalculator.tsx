import PlaceholderPage from "@/components/PlaceholderPage";
import { Calculator } from "lucide-react";
export default function ROICalculator() {
  return <PlaceholderPage title="ROI Calculator" description="Calculate return on investment for climate resilience infrastructure projects." icon={Calculator} />;
}
